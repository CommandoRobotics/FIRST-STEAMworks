package AutnomousPrograms;

import APIs.Chassis;
import AutonomousCommands.Delay;
import AutonomousCommands.Tests.LeftFront;
import AutonomousCommands.Tests.LeftRear;
import AutonomousCommands.Tests.RightFront;
import AutonomousCommands.Tests.RightRear;
import edu.wpi.first.wpilibj.Timer;
import edu.wpi.first.wpilibj.command.CommandGroup;

public class WheelTest extends CommandGroup{

	public WheelTest(Chassis chassis, Timer timer) {
//		addSequential(new LeftFront(chassis, timer, 2.0));
//		addSequential(new Delay(chassis, timer, 4.0));
//		addSequential(new RightFront(chassis, timer, 6.0));
//		addSequential(new Delay(chassis, timer, 8.0));
//		addSequential(new LeftRear(chassis, timer, 10.0));
//		addSequential(new Delay(chassis, timer, 12.0));
//		addSequential(new RightRear(chassis, timer, 14.0));
//		addSequential(new Delay(chassis, timer, 15.0));
	}
	
}
